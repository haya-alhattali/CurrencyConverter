<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.haya.currencyconverter1">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurrencyConverter1"
        android:targetSdkVersion="33"
        tools:targetApi="31"
        tools:ignore="ExtraText,OldTargetApi">

        <!-- Login Activity -->
        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".HomepageActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".CurrencyConversionActivity"
            android:exported="false">
        </activity>

    </application>

    <!-- SDK Versions -->
    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="33"
        tools:ignore="GradleOverrides,ManifestOrder,OldTargetApi" />

</manifest>